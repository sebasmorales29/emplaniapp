name: Deploy ASP.NET MVC (.NET Framework) to Azure

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      # Localiza la solución y el proyecto Web clásico (.NET Framework)
      - name: Locate solution and web project
        shell: pwsh
        run: |
          Write-Host "Buscando .sln y .csproj…"
          $sln = Get-ChildItem -Recurse -Filter *.sln | Select-Object -First 1
          if (-not $sln) { throw "No .sln found in repository." }
          "SOLUTION_PATH=$($sln.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append

          $web = Get-ChildItem -Recurse -Filter *.csproj | Where-Object {
            (Select-String -Path $_.FullName -Pattern "<ProjectTypeGuids>|<UseIISExpress>|Microsoft.WebApplication.targets" -Quiet)
          } | Select-Object -First 1
          if (-not $web) { throw "No Web .csproj found." }
          "WEBPROJ_PATH=$($web.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append

          Write-Host "SOLUTION_PATH=$env:SOLUTION_PATH"
          Write-Host "WEBPROJ_PATH=$env:WEBPROJ_PATH"

      - name: Restore NuGet packages
        shell: pwsh
        run: nuget restore "$env:SOLUTION_PATH"

      - name: Build (Release)
        shell: pwsh
        run: msbuild "$env:SOLUTION_PATH" /p:Configuration=Release

      # Publica a carpeta (FileSystem)
      - name: Publish to folder (FileSystem)
        shell: pwsh
        run: |
          $pub = Join-Path $env:GITHUB_WORKSPACE 'publish'
          if (Test-Path $pub) { Remove-Item $pub -Recurse -Force }
          New-Item -ItemType Directory -Path $pub | Out-Null

          msbuild "$env:WEBPROJ_PATH" `
            /p:Configuration=Release `
            /p:WebPublishMethod=FileSystem `
            /p:DeleteExistingFiles=True `
            /p:PublishUrl="$pub"

      # Comprime la salida publicada en artifacts/site.zip
      - name: Create site zip
        shell: pwsh
        run: |
          $pub = Join-Path $env:GITHUB_WORKSPACE 'publish'
          $art = Join-Path $env:GITHUB_WORKSPACE 'artifacts'
          New-Item -ItemType Directory -Force -Path $art | Out-Null

          $zipPathWin = Join-Path $art 'site.zip'
          if (Test-Path $zipPathWin) { Remove-Item $zipPathWin -Force }
          Compress-Archive -Path "$pub\*" -DestinationPath $zipPathWin

          # Exporta variable con RUTA RELATIVA usando barras normales (para el glob del action)
          "PACKAGE_PATH=artifacts/site.zip" | Out-File -FilePath $env:GITHUB_ENV -Append

          Write-Host "ZIP creado en: $zipPathWin"
          dir $art

      # Verifica que el zip exista donde espera el action
      - name: Verify package path
        shell: pwsh
        run: |
          Write-Host "PACKAGE_PATH=$env:PACKAGE_PATH"
          if (-not (Test-Path (Join-Path $env:GITHUB_WORKSPACE $env:PACKAGE_PATH))) {
            Write-Host "Contenido de artifacts:"
            dir (Join-Path $env:GITHUB_WORKSPACE 'artifacts') -Force
            throw "Zip not found at $env:PACKAGE_PATH"
          }

      # Despliegue a Azure App Service (Zip Deploy)
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: Emplaniapp
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
          package: ${{ env.PACKAGE_PATH }}
